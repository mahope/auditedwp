# Cloudflare Pages configuration for EUComply
# Deploy: wrangler pages deploy site/ --project-name=eucomply
# (Requires `wrangler login` first, or use --temporary for preview)

name = "eucomply"
compatibility_date = "2026-08-23"

[pages]
  build_command = ""
  build_output_dir = "site"